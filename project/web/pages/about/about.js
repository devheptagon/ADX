import styles from "styles/home.module.scss";

export default function About() {
  return (
    <div className={styles.outer}>
      <div className={styles.inner}>here is the About</div>
    </div>
  );
}
